Не хранимые структуры:
```1C
@ВПодсистеме
метод Разобрать(ФИО: Строка)
    знч массивФИО = ФИО.Разделить()
    если массивФИО.Размер() == 3
       Фамилия =  
```

```
метод НаименованиеПриИзменении(...)
 знч ФИО = новый ФИО()
 ФИО.Разобрать(Объект.Наименование)
 Объект.Фамилия = ФИО.Фамилия
 Объект.Имя = ФИО.Имя
 Объект.Отчество = ФИО.Отчество
```

Хранимые структуры:
```1c
@ВПодсистеме
@Подписка(Событие{Партнеры.Обьект.ПослеЗаписи})
метод ПослеЗаписиОбъектаЖурнала(Источник: Партнеры.Данные, До: Партнеры.Данные, ПараметрыЗаписи: Партнеры.ПараметрыЗаписи)
 ОбщийЖурналОбъектов.ДобавитьОбъект(Источник.Ссылка, Источник.Ссылка.Представление())
;

@ВПодсистеме
@Подписка(Событие{Контакты.Обьект.ПослеЗаписи})
метод ПослеЗаписиОбъектаЖурнала(Источник: Контакты.Данные, До: Контакты.Данные, ПараметрыЗаписи: Контакты.ПараметрыЗаписи)
 ОбщийЖурналОбъектов.ДобавитьОбъект(Источник.Ссылка, Источник.Ссылка.Представление())
;
...
```

```
@Обработчик
метод ПослеЗаписи()
    если ЭтоНовый()
       знч Событие = новый СозданиеДокумента(Представление = "Номер Заявки: %Номер )
;
```

Форма списка и форма объектов
